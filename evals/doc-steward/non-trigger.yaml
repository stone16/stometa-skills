schema_version: 1
source: synthetic
cases:
  - id: prose-copy-edit
    prompt: Tighten the prose and tone of this product announcement.
    expected:
      activates: false
      reason: prose editing is outside repository documentation governance
  - id: application-code-review
    prompt: Review this Python service for concurrency bugs.
    expected:
      activates: false
      reason: application code review is outside scope
  - id: generic-formatting
    prompt: Run the Markdown formatter over every note.
    expected:
      activates: false
      reason: generic formatting is outside scope
  - id: interface-design
    prompt: Design a polished analytics dashboard for this application.
    expected:
      activates: false
      reason: product interface design is outside scope
  - id: product-doc-link-check
    prompt: Check the links in our customer-facing product manual.
    expected:
      activates: false
      reason: generic product-document link checking is outside agent-facing repository documentation governance
  - id: pull-request-delivery
    prompt: Commit the current application changes, push the branch, and open a pull request.
    expected:
      activates: false
      reason: generic Git delivery does not invoke documentation stewardship
