# Skill evaluations

Each stable skill has an `evals/<skill-name>/` directory containing trigger, non-trigger, and behavior cases. Recorded fixtures, real model runs, human review, and production observations must be labeled separately.

