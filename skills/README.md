# Public skills

This directory is intentionally empty during repository incubation.

Only skills accepted through [the promotion policy](../docs/promotion-policy.md) are added here. A promoted skill may later remain with lifecycle `deprecated` during its migration window, but default collections exclude it. Do not create placeholder skills to make an installer or plugin manifest appear functional.
