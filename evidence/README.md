# Promotion evidence

Each stable skill has one `evidence/<skill-name>.yaml` file conforming to `schemas/promotion-evidence.schema.json`.

Evidence files contain methods, counts, dates, and anonymized context identifiers. Raw prompts, outputs, private repository names, and usage receipts remain private.

